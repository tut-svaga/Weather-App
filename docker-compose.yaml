version: '5.1.3'
services:
  backend:
    image: weather_backend
    build: 
      context: ./backend
    container_name: weather_app_back
    ports:
      - "${APP_PORT}:8000"
    env_file: .env
    volumes:
      - ./backend:/app