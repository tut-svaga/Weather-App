# npm run dev — напрямую к backend (порт из .env BACK_PORT=8000):
# VITE_API_BASE_URL=http://localhost:8000

# или через Vite proxy:
# VITE_API_BASE_URL=/api

# prod/docker — оставь пустым, будет /api (через nginx)
