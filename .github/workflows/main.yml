name: Git Actions

on: 
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
jobs:
  weather:
<<<<<<< HEAD
    runs-on: ubuntu-latestmatrix.os }}
=======
    runs-on: ubuntu-latest

on:
  push: 
    branches: 
      - main
      - dev
  pull_request:
    branches: 
      - main
      - dev
jobs:
  weather:
    runs-on: ${{ matrix.os }}
>>>>>>> d6ec99bd1bb0cd47b5d24ea1c098f50df032a6f6

    steps:
      - name: CheckOut
        uses: actions/checkout@v4  

      - name: Set Up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: test Docker (backend)
        uses: docker/build-push-action@v6
        with: 
          context: ./backend
          file: ./backend/Dockerfile
          push: false
      
      
      
      
      
